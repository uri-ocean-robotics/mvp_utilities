# JOT THRUSTER MAP.

## Introduction
This package takes in Joy message and returns respective thruster commands.

## Requirements
A respective control mode under Teleoperation behaviour in `helm.yaml` with corresponding thruster links disabled/enabled in `control.yaml`